<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Answers - <%= question.title %> | Ummah Hub</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/css/view-answers.css">
    <link href="https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body class="pattern-bg">
    <%- include('partials/header') %>

    <main class="container">
        <section class="question-container">
            <h2><%= question.title %></h2>
            <div class="question-body">
                <%= question.body %>
            </div>
            <div class="question-meta">
                <span>Posted by <strong><%= question.author %></strong></span>
                <span>•</span>
                <span><%= new Date(question.created_at).toLocaleString('en-US', { 
                    year: 'numeric',
                    month: 'short',
                    day: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                }) %></span>
            </div>
        </section>

        <!-- List of Answers -->
        <section class="answers-container">
            <h3>Community Answers</h3>
            <% if (answers.length === 0) { %>
                <div class="empty-state">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>
                    </svg>
                    <p>No answers yet. Be the first to share your knowledge!</p>
                    <% if (!userId) { %>
                        <p>Please <a href="/users/login">log in</a> to submit your answer.</p>
                    <% } %>
                </div>
            <% } else { %>
                <% answers.forEach(answer => { %>
                    <div class="answer">
                        <div class="answer-content">
                            <%= answer.answer %>
                        </div>
                        <div class="answer-meta">
                            <span>Answered by <strong><%= answer.answer_author %></strong></span>
                            <span>•</span>
                            <span><%= new Date(answer.created_at).toLocaleString('en-US', { 
                                year: 'numeric',
                                month: 'short',
                                day: 'numeric',
                                hour: '2-digit',
                                minute: '2-digit'
                            }) %></span>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </section>
        
        <div style="text-align: center;">
            <a href="/qaForum/<%= category %>" class="back-btn">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M19 12H5M12 19l-7-7 7-7"/>
                </svg>
                Back to Forum
            </a>
        </div>
    </main>

    <%- include('partials/footer') %>
</body>
</html>

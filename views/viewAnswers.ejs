<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Answers - Ummah Hub</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1 class="title">Ummah Hub</h1>
        <nav>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/specialIslamicDays">Islamic Days</a></li>
                <li class="dropdown">
                    <a href="/qaForum">Forums</a>
                    <ul class="dropdown-menu">
                        <li><a href="/qaForum">Q&A Forum</a></li>
                        <li><a href="/duaForum">Dua Forum</a></li>
                    </ul>
                </li>
                <li><a href="/chatbot">Chatbot</a></li>
                <li><a href="/Zakat">Zakat</a></li>
                <li><a href="/Prayer Times">Prayer Times</a></li>
                <% if (!userId) { %>
                    <li class="nav-right"><a href="/users/login" class="login-btn">Login</a></li>
                <% } else { %>
                    <li class="nav-right"><a href="/users/logout" class="logout-btn">Logout</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <main>
        <section class="question-container">
            <h2><%= question.title %></h2>
            <p><%= question.body %></p>
            <small>Posted by <strong><%= question.author %></strong> on <%= question.created_at.toLocaleString() %></small>
        </section>

        <!-- List of Answers -->
        <section class="answers-container">
            <h3>All Answers</h3>
            <% if (answers.length === 0) { %>
                <p>No answers yet. Be the first to answer!</p>
            <% } else { %>
                <% answers.forEach(answer => { %>
                    <div class="answer">
                        <p><%= answer.answer %></p>
                        <small>Answered by <strong><%= answer.answer_author %></strong> on <%= answer.created_at.toLocaleString() %></small>
                    </div>
                <% }) %>
            <% } %>
        </section>
        
        <a href="/qaForum/<%= category %>" class="back-btn">Back to Forum</a>
    </main>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> Ummah Hub. All rights reserved.</p>
    </footer>
</body>
</html>
